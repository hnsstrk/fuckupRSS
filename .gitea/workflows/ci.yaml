name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Stage 1: Lint & Type Check
  lint:
    name: Lint & Type Check
    runs-on: linux-x64
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint

      - name: Prettier check
        run: npm run format:check

      - name: Svelte check
        run: npx svelte-check --tsconfig ./tsconfig.json

      - name: TypeScript check
        run: npx tsc --noEmit

  rust-lint:
    name: Rust Lint
    runs-on: linux-x64
    steps:
      - uses: actions/checkout@v4

      - name: Rust format check
        run: cargo fmt --manifest-path src-tauri/Cargo.toml -- --check

      - name: Clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings

  # Stage 2: Tests (parallel)
  test-frontend:
    name: Frontend Tests
    runs-on: linux-x64
    needs: [lint]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Vitest
        run: npm run test

  test-rust-linux:
    name: Rust Tests (Linux)
    runs-on: linux-x64
    needs: [rust-lint]
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib --bins

  test-rust-macos:
    name: Rust Tests (macOS)
    runs-on: macos-arm64
    needs: [rust-lint]
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib --bins

  test-e2e:
    name: E2E Tests
    runs-on: linux-x64
    needs: [lint]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run E2E tests
        run: npm run test:e2e

  # Stage 3: Security
  security:
    name: Security Scan
    runs-on: linux-x64
    needs: [test-frontend, test-rust-linux]
    steps:
      - uses: actions/checkout@v4

      - name: Semgrep scan
        run: semgrep scan --config auto --error src-tauri/src/ src/

      - name: npm audit
        run: npm audit --audit-level=high

  # Stage 4: Build (parallel)
  build-linux:
    name: Build (Linux)
    runs-on: linux-x64
    needs: [test-frontend, test-rust-linux, security]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run tauri build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/appimage/*.AppImage

  build-macos:
    name: Build (macOS)
    runs-on: macos-arm64
    needs: [test-rust-macos, security]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run tauri build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: src-tauri/target/release/bundle/dmg/*.dmg

  # Stage 5: SBOM
  sbom:
    name: Generate SBOM
    runs-on: linux-x64
    needs: [build-linux]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Generate Frontend SBOM
        run: npx @cyclonedx/cyclonedx-npm --output-file sbom/frontend-bom.json

      - name: Generate Backend SBOM
        run: |
          cd src-tauri
          cargo cyclonedx --format json
          mv fuckuprss.cdx.json ../sbom/backend-bom.json

      - name: Validate SBOMs
        run: |
          cyclonedx validate --input-file sbom/frontend-bom.json
          cyclonedx validate --input-file sbom/backend-bom.json

      - name: Upload SBOMs
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom/*.json
